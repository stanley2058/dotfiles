title Monitor
layout fat
launch htop
launch watch -n 1 sensors
launch watch -n 1 "cat /proc/cpuinfo | grep 'MHz' | awk 'BEGIN {max=0} { sum+=$4; n++; str=sprintf(\"%s\n%s\",str,$0); if ($4>max) max=$4 } END { if (n > 0) print \"Avg \" sum / n \" MHz\"; print \"Max \"max\" MHz\"; print str } '"
